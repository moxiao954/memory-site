# 个人 AI 学习分享网站 · 详细需求文档（PRD v2.0）

> 文档版本：v2.0（最终版）
> 编制角色：产品经理（nimbus）
> 文档日期：2026-08-02
> 项目状态：需求已冻结，等待实施
> 实施方式：由另一 Agent（实施 Agent）按本文档执行

## 目录
1. 项目概述
2. 目标用户与使用场景
3. 功能需求总览
4. 页面设计
5. 用户旅程
6. 用户故事
7. 实现逻辑
8. 功能细节与边界条件
9. 权限与安全
10. 风险与对策
11. 验收标准
12. 实施要求（Agent 工作指引）
13. 后续迭代方向

## 1. 项目概述
### 1.1 背景
- 作者：非程序员、0 编程基础、不懂英文的 AI 学习初学者。
- 需求：拥有一个"个人网页"，公开分享自己的 AI 学习过程。
- 受众：同样 0 基础学 AI 的同路人，作为借鉴参考。

### 1.2 一句话定位
一个深色科幻风格的静态个人学习分享网站：记录"我什么时候学了什么"，沉淀笔记与项目，供同路人访问借鉴。### 1.3 已确认决策清单（重要，勿偏离）
| 编号 | 决策项 | 结论 |
|---|---|---|
| D1 | 目标用户 | 同样 0 基础学 AI 的同路人（借鉴参考） |
| D2 | 内容语言 | 全中文；技术名词首次出现配英文原文（如：静态网站 static website） |
| D3 | 托管方式 | GitHub Pages 免费托管 + 免费域名（xxx.github.io） |
| D4 | 设计风格 | 深色科幻（Dark Sci-Fi） |
| D5 | 首页布局 | 学习日记概览简化为时间线；去掉"最新文章"栏目 |
| D6 | 学习日记 | 横向时间线，从左到右按时间排列；本期只创建模块、不填充真实记录 |
| D7 | 移动端适配 | 不做（桌面端优先） |
| D8 | 评论功能 | 不做 |
| D9 | 项目目录 | 本期不写入项目目录（仓库内文件结构见第 7 节） |
| D10 | 作者侧访问 | 作者使用 fastGitHub 工具访问 GitHub 与 github.io |
| D11 | 域名 | 使用 GitHub 免费子域名（xxx.github.io），不付费购买域名 |
| D12 | 时间线动画 | 页面加载时节点从左到右依次点亮（简单动画） |

### 1.4 非目标（明确不做）
- 不接入付费服务、不购买域名。
- 不引入数据库、后端服务、前端框架。
- 不做评论、搜索、登录、多语言、移动端适配。

## 2. 目标用户与使用场景
- 主用户：网站作者本人（记录者、管理者）。
- 访客：同样 0 基础学 AI 的同路人。
- 场景 A：作者记录——某天学了某知识点，打开文件添加一条记录并上传。
- 场景 B：访客借鉴——看到作者的学习时间线，判断路径是否值得参考，阅读笔记与项目。## 3. 功能需求总览
网站共 5 个页面（桌面端）：
| 页面 | 文件 | 内容 |
|---|---|---|
| 首页 | index.html | Hero + 学习日记概览（简版时间线）+ 精选项目 + 页脚 |
| 关于我 | about.html | 作者简介、学习背景、联系方式 |
| 项目作品集 | projects.html | 项目卡片（暂无项目时放占位卡） |
| 学习日记 | diary.html | 横向时间线（核心模块，本期只建模块不填数据） |
| 博客/文章 | blog.html | 学习笔记、踩坑记录、技术分享 |

## 4. 页面设计
### 4.1 信息架构
首页 index.html
├── ① 关于我 about.html（简介/联系方式）
├── ② 项目作品集 projects.html（项目卡片）
├── ③ 学习日记 diary.html（横向时间线）
└── ④ 博客/文章 blog.html（笔记与分享）
导航栏固定在页面顶部，5 个入口：首页、关于我、项目、学习日记、博客。

### 4.2 首页布局（自上而下）
1. Hero 区：一句话自我介绍 + 按钮"查看我的学习日记"（点击跳转 diary.html）。
2. 学习日记概览：横向时间线简版，展示最近 3~5 个节点，点击"查看全部"进入学习日记页。
3. 精选项目：项目卡片（暂无项目时放 1 张"建设中"占位卡）。
4. 页脚：联系方式（邮箱或社交媒体链接，先占位）。
注意：首页不设"最新文章"栏目（已确认去掉）。### 4.3 学习日记页 · 横向时间线（核心模块）
#### 4.3.1 布局形态
- 一条水平线贯穿页面中部，节点从左到右按时间递增排列（最早在左、最近在右）。
- 节点卡片在时间线的上、下交错错落排布（类似折线图高低分布）。
- 节点数量较多时，时间线区域可横向滚动，不换行。
#### 4.3.2 节点内容
每个节点包含 4 个字段：
- 时间（如 2026.08）
- 标题：学了什么（一句话）
- 描述：一两句补充说明
- 产出链接（可选）：如对应的文章或项目页链接
#### 4.3.3 交互与动画
- 悬停：节点卡片青色高亮 + 发光边框。
- 加载动画（已与作者确认）：页面加载时，节点从最左边开始依次点亮——每个节点由暗淡变亮并发光，间隔约 0.3 秒，仅播放一次不循环。
- 动画目的：引导访客按时间顺序（左到右）浏览，增强科技感。
- 实现方式：给每个节点设置递增的延迟启动时间（0s、0.3s、0.6s...），配合透明度与发光过渡；节点多时缩短间隔，保证总时长不超过 2 秒。
#### 4.3.4 本期状态
- 模块结构完整可展示，但"只创建、不记录"：放 1 个示例节点 + "待开始记录"提示占位，不填真实学习内容。
- 数据维护方式见 7.4。### 4.4 其余页面设计（简）
- 关于我：头像/占位图 + 自我介绍段落 + 联系方式（邮箱/链接占位）。
- 项目作品集：网格卡片布局；卡片含项目名、一句话简介、状态标签（如"规划中/建设中"）；无项目时显示 1 张占位卡。
- 博客：文章列表（标题 + 日期 + 摘要），文章详情页第一版不做，列表链接先占位。

### 4.5 视觉规范（深色科幻，全站统一）
| 元素 | 规范 |
|---|---|
| 背景 | 深蓝黑渐变（#0B0F1A → #101830）+ 微星点/网格线 |
| 强调色 | 青色 #00E5FF + 紫色 #7C4DFF（按钮、链接、节点高亮） |
| 标题 | 轻微霓虹发光效果 |
| 正文 | 常规字体（如系统默认/微软雅黑），保证可读性 |
| 卡片 | 半透明深色底 + 悬停发光边框 |
| 导航 | 顶部固定，半透明深色底 + 青色高亮当前页 |
- 全站样式集中在 css/style.css，页面间保持一致。
- 页面最小宽度约 1200px，不做响应式/移动端适配。

## 5. 用户旅程
旅程 A（作者·记录者）：打开 fastGitHub → 修改网页文件 → GitHub Desktop 提交并推送 → 网站自动更新 → 看到学习日记变长 → 获得成就感、坚持记录。
旅程 B（访客·同路人）：输入网址访问 → 浏览首页了解作者 → 进入学习日记页（节点依次点亮引导阅读）→ 查看博客与项目 → 通过联系方式交流。## 6. 用户故事
1. 作为访客，我希望一眼看到作者的学习时间线，这样我能判断这条学习路径是否值得借鉴。
2. 作为作者，我希望改一个文件就能更新网站，这样我不用学复杂技术也能坚持更新。
3. 作为作者，我希望打开 fastGitHub 就能正常访问和管理网站，这样我不会被网络问题卡住。
4. 作为作者，我希望只有我能修改内容，这样我不用担心网站被外人改坏。
5. 作为访客，我希望网站能在国内网络下打开，这样我才愿意收藏和推荐。

## 7. 实现逻辑
### 7.1 技术路线（静态网站 static website）
HTML + CSS + 少量 JavaScript → GitHub Desktop 上传 → GitHub Pages 免费托管 → xxx.github.io 访问。
明确不用：数据库、后端语言、前端框架、构建工具。

### 7.2 仓库文件结构（本期不写入项目目录，仅仓库内结构）
memory-site/
├── index.html          # 首页
├── about.html          # 关于我
├── projects.html       # 项目作品集
├── diary.html     # 学习日记（横向时间线）
├── blog.html           # 博客
├── css/style.css       # 深色科幻样式（全站共用）
├── js/timeline.js      # 时间线数据与动画
└── images/             # 图片目录（先留空或放占位图）### 7.3 发布与更新流程
首次搭建：
1. 作者注册/登录 GitHub（英文界面可接受，实施 Agent 可引导或代为操作）。
2. 新建仓库（repository），仓库名建议 memory-site。
3. 上传 7.2 中全部文件。
4. 开启 GitHub Pages（Settings → Pages → 选择分支发布）。
5. 获得网址 https://作者用户名.github.io/memory-site/（若仓库名与用户名一致则为根路径）。
日常更新（作者操作）：
1. 修改对应网页文件（本地）。
2. 打开 GitHub Desktop：填写说明 → 点击提交（Commit）→ 点击推送（Push）。
3. GitHub Pages 自动更新网站，访客访问到新版。

### 7.4 学习日记数据维护
- 数据集中放在 js/timeline.js 中，为一个按时间升序排列的数组。
- 新增记录：按时间顺序添加一行即可，例如：
  { time: "2026.08", title: "搭建个人网站", desc: "学习网页基础", link: "" }
- 渲染：diary.html 引入 timeline.js，自动生成横向节点；无需改 HTML。
- 本期：数组内仅保留 1 条示例数据 + 空状态提示。## 8. 功能细节与边界条件
| 项 | 说明 |
|---|---|
| 时间线数据结构 | 数组按时间升序；每项含 time（年月）、title（学了什么）、desc（描述）、link（可选产出链接） |
| 空数据 | timeline.js 无数据时，页面显示"待开始记录"提示，时间线框架仍可见 |
| 数据量大 | 时间线区域横向滚动，不换行；加载动画间隔自动缩短，总时长不超过 2 秒 |
| link 为空 | 该节点不显示链接图标，其余正常 |
| 示例节点 | 必须有 1 条示例数据用于演示效果，标注"示例"字样，便于日后删除 |
| 页面宽度 | 桌面端固定最小宽度约 1200px；不做移动端适配 |
| 内容语言 | 全中文；技术名词首次出现配英文原文 |
| 导航高亮 | 当前所在页面在导航中青色高亮 |
| 图片缺失 | 页面不因缺图而报错，使用占位样式 |

## 9. 权限与安全
- 线上只读：GitHub Pages 为纯静态网站，访客只能读取页面，无任何编辑/上传入口。
- 写权限独占：上传/修改必须使用作者本人 GitHub 账号；不添加任何协作者（collaborator）。
- 说明：免费版 GitHub Pages 要求仓库公开，源码（HTML/CSS/JS）访客可查看/下载，但无法改动线上网站；学习分享内容本身公开，无泄密风险。
- 作者侧网络：作者通过 fastGitHub 工具保证自身可正常访问 GitHub 与 github.io。## 10. 风险与对策
| 风险 | 影响 | 对策 |
|---|---|---|
| github.io 国内访问不稳定 | 访客可能打不开网站 | 上线后请多位朋友在不同网络实测；如不稳定，后续绑定 Cloudflare CDN（免费）加速，或迁移 Gitee Pages（需实名认证，作为备选） |
| fastGitHub 仅解决作者侧访问 | 访客不会安装该工具，访客侧访问问题仍在 | 以访客实测结果为准；网站本身不依赖该工具 |
| 作者不熟悉 GitHub 操作 | 更新流程卡壳 | 使用 GitHub Desktop 图形界面（不敲命令）；实施 Agent 提供图文步骤清单 |
| 仓库名/用户名不一致导致网址变长 | 网址不够简洁 | 仓库名与 GitHub 用户名保持一致可缩短网址；第一版可接受 |
| 时间线数据格式写错 | 页面显示异常 | 按 7.4 模板添加；实施时在 timeline.js 中提供注释说明 |

## 11. 验收标准（实施 Agent 必须逐项验证）
1. 5 个页面均可通过 github.io 网址访问。
2. 页面为深色科幻风格，符合 4.5 视觉规范。
3. 学习日记页：横向时间线展示正常；加载时节点从左到右依次点亮；示例节点可见；无真实记录。
4. 首页无"最新文章"栏目；学习日记概览为简版时间线。
5. 修改一个文件并推送后，网站自动更新（用 playwright 实测验证）。
6. 访客视角无任何修改/上传入口。
7. 全站内容为中文，技术名词首次出现配英文原文。## 12. 实施要求（Agent 工作指引）
### 12.1 实施方式
- 本项目由另一 Agent（下称"实施 Agent"）负责完成，按本文档逐节实现。
- 实施 Agent 应遵循"先计划、再执行、后验证"的流程；遇到需求不明确时，先向用户确认，不擅自变更已确认决策（见 1.3）。
- 实施过程中应使用配置好的工具与技能（见 12.2~12.4），并"在需要时及时使用"，避免不用或过度使用。

### 12.2 MCP 服务器使用指引（需要时及时使用）
| MCP 服务器 | 用途 | 使用时机 |
|---|---|---|
| playwright | 浏览器自动化：打开网页、点击、截图、验证 | 网站开发完成后，用真实浏览器逐页验证布局、时间线动画、导航跳转；模拟访客视角检查"无修改入口" |
| open-websearch | 开放网络搜索 | 查询 GitHub Pages 最新配置方法、深色科幻 CSS 效果参考等 |
| sequential-thinking | 顺序思考/复杂问题拆解 | 遇到多步骤、多依赖的实现问题时，逐步推理后再动手 |
| memory | 长期记忆 | 保存关键决策（D1~D12）、已完成步骤，跨会话保持上下文 |
| context7 | 库/框架文档查询 | 查询 HTML/CSS/JS 或 GitHub Pages 相关 API 与最佳实践 |
| tavily | 搜索（备选） | 与 open-websearch 二选一或互补，用于资料查询 |
| docx-editor | 文档编辑 | 如需输出/更新 Word 版本文档（本 PRD 的 .docx 副本） |### 12.3 Skills 使用指引（需要时及时使用）
| Skill | 用途 | 使用时机 |
|---|---|---|
| brainstorming | 头脑风暴 | 设计方案前，快速列出可行方案（如时间线动画实现方式） |
| writing-plans | 编写计划 | 动手前输出实施计划（页面清单、步骤、验证方式） |
| executing-plans | 执行计划 | 按 writing-plans 的计划逐步执行并跟踪进度 |
| grill-me / grilling | 反问/审查需求 | 实施前用关键问题自审需求（如"动画是否需要循环？""示例节点是否标注？"） |
| subagent-driven-development | 子代理开发 | 将独立子任务（如 CSS 主题、timeline.js）拆给子代理并行开发 |
| systematic-debugging | 系统化调试 | 页面渲染异常、动画不生效时，先定位根因再修复 |
| test-driven-development | 测试驱动开发 | 先写验收用例（如"时间线数组渲染 N 个节点"）再实现 |
| using-superpowers | 综合技能管理 | 全局统筹使用以上技能 |
| verification-before-completion | 完成前验证 | 交付前对照第 11 节验收标准逐项验证 |
| deep-research | 深度研究 | 对复杂主题（如国内访问 github.io 的可靠方案）做深入研究后给建议 |### 12.4 CLI 工具使用指引（需要时及时使用）
| CLI 工具 | 用途 | 使用时机 |
|---|---|---|
| gh（GitHub CLI） | 命令行操作 GitHub：创建仓库、开启 Pages、推送 | 创建仓库/配置 Pages 时优先使用（比网页点按更可靠可复现） |
| git | 版本管理：提交、推送、分支 | 本地文件变更后的提交与推送；配合 GitHub Desktop 或直接命令行 |
| java / javac | Java 运行时/编译（环境自带，本期一般用不到） | 仅当所选工具链需要 Java 时使用；本期预计不需要 |
| opencli | 开放能力 CLI（按环境实际能力） | 需要系统级操作（如本地起临时 HTTP 服务器预览网站）时使用 |
| 其他可用命令 | PowerShell / cmd / node / python | 本地预览网站（如 python -m http.server）、静态检查等 |
- 注意：实施 Agent 所在环境为 Windows（PowerShell 默认），项目相关路径见用户指定目录。
- 使用 gh 前确认已登录（gh auth status），未登录先引导登录。

### 12.5 建议实施流程（Agent 参考）
1. 读本文档 → 用 sequential-thinking 梳理任务 → 用 writing-plans 写计划。
2. 用 grill-me 自审需求，向用户确认遗留疑问。
3. 建仓库（gh repo create）→ 写 5 个页面 + style.css + timeline.js。
4. 本地预览验证（python -m http.server 或 playwright 打开本地文件）。
5. 推送并开启 GitHub Pages（gh 或网页）。
6. 用 playwright 打开线上网址，对照第 11 节逐项验收。
7. 用 verification-before-completion 确认后交付用户，附使用说明。## 13. 后续迭代方向（本期不做，仅记录）
- 付费自定义域名（.com/.top 等）替换免费子域名。
- 国内访问不稳定时：Cloudflare CDN 加速或迁移 Gitee Pages。
- 评论功能（需引入第三方评论服务，如 giscus）。
- 移动端适配。
- 博客文章详情页。
- 学习日记真实数据填充（作者后续按 7.4 添加）。

---
（文档结束）
本 PRD v2.0 由产品经理（nimbus）编制，需求已与作者逐项确认（决策清单见 1.3）。
实施 Agent 如发现与本文档冲突的内容，以本文档为准；如需变更，先与作者确认。
